# 改进反馈（Feedback）

> 本文件记录《泡泡小英雄》历次改进需求与实施状态。
> 约定：新的改进需求请追加到「待办」区；实现并验证后移入「已实现」区并标注日期与涉及模块。

---

## 已实现

### 2026-08-16 · v1.5.10 手机 HUD 再往左贴 + 上线 GitHub Pages（本轮）

| 需求 | 实施 | 状态 |
|---|---|---|
| 手机端 UI 还是和地图左侧重合，把它往左放 | `body.is-touch #hud` 定位简化为 `left: max(8px, calc((100vw - 100vh×15/13) / 2))`（去掉可能解析失败的 `min()` 嵌套），盒子收窄：max-width 172→118px、字号 12→10px、红心 10px、间距收紧——小屏（SE 横屏留白 ~110px）也不压地图 | ✅ |
| 传到 GitHub 生成 Web 链接 | 建公开仓库 `HollowKt5/bombman`，本地 `git init` + `.gitignore`（node_modules/dist）+ 复制 `dist/index.html → index.html` + 提交推送，开启 GitHub Pages（main/root）：**https://hollowkt5.github.io/bombman/**（已验证 HTTP 200） | ✅ |

### 2026-08-16 · v1.5.9 手机版 UI 收进左侧矩形 + 像素红心 + 整体像素风（本轮）

| 需求 | 实施 | 状态 |
|---|---|---|
| 只改手机版 UI：整体放在一个矩形里、位于地图左侧、不挡虚拟摇杆也不挡地图 | `body.is-touch #hud` 改为**左上角紧凑矩形**（flex 纵向、内容自动换行、max-width 172px）；`left: max(10px, calc((100vw - min(100vw, 100vh×1.154)) / 2 + 10px))` —— 15:13 地图高度撑满时自动落在**地图左侧留白**里，横向撑满时贴左上角；与左下虚拟摇杆（bottom:26px）不重叠 | ✅ |
| 红心改为像素版（像塞尔达老像素版一样） | HUD 生命改为 **8-bit 像素心**：`pixelHeart()` 生成 7×6 像素矩阵 SVG（`shape-rendering="crispEdges"`），实心=红心、空心=灰心（双端生效，不再是 ❤️/🤍 emoji） | ✅ |
| 整体 UI 风格朝像素方向靠 | 双端：HUD 深色硬边面板（圆角 999px→4px、2px 边框、硬阴影）+ 等宽数字字体；面板 24px→8px 圆角 + 3px 边框 + 硬阴影；按钮/难度选项硬角化；标题像素硬阴影；放泡键圆→像素方块（8px 圆角 + 边框 + 硬阴影） | ✅ |

验证：88 项单测全过、typecheck 干净；单文件构建 69.9 kB

### 2026-08-16 · v1.5.8 爆炸震动/特效调小 + 禁用双击缩放 + 战败钢琴悲情音乐（本轮）

| 需求 | 实施 | 状态 |
|---|---|---|
| 爆炸震动太大 | `explosion:start` 屏幕震动 `addShake(4)` → **1.5**（衰减与阈值不变） | ✅ |
| 爆炸特效太大（黄圈圈） | 冲击波黄圈：半径 `(0.35+1.7×(1-r))` → **`(0.28+0.8×(1-r))`**、线宽 `cs×0.07` → **`cs×0.035`**（内圈随之缩小）；爆炸主体格 `cs×0.94` → **`cs×0.8`**；💥 字号 `cs×0.75` → **`cs×0.6`**；爆炸粒子每格 3→**2**、速度 190→140、尺寸 2~5→1.5~3.5 | ✅ |
| 手机端双击误触放大，禁用浏览器缩放 | ①`touchstart`（非被动）阻止默认：**交互元素（按钮/输入/标签/摇杆）保留，其余触摸 preventDefault** → 双击不放大、按钮仍可点；②`dblclick`/`gesturestart`/`gesturechange` preventDefault；③CSS 交互元素加 `touch-action: manipulation`（叠加原有 `user-scalable=no` + body `touch-action:none`） | ✅ |
| 写个战败音乐，带钢琴、悲情一点，音量 -50% | `WebAudioSynth.playDefeatMusic()`：**D 小调下行哀歌**（C5→B4→A4→G4→A4→F4→G4→A4→G4→F4→E4→D4，结尾拉长），钢琴音色=三角波主音+高八度泛音，Dm→♭B→F→Dm 低音根音铺垫；**单音音量 0.015 = 主 BGM 的 50%**；`game:over` 时先 `lose()` 短促下滑音再播悲情曲（约 10s） | ✅ |

验证：88 项单测全过；typecheck 干净；单文件构建 68.5 kB

### 2026-08-16 · v1.5.7 每局 5 分钟 + AI 炸软块掉落缩水（本轮）

| 需求 | 实施 | 状态 |
|---|---|---|
| 每局时间改为 5 分钟 | 难度表 `timeLimitTicks` 统一为 `300 × 60 = 18000` tick（EASY/NORMAL/HARD 一致，HUD 显示 05:00） | ✅ |
| AI 炸出坐骑概率 -2/3 | 新增 **AI 掉落表** `BLOCK_DROP_TABLE_AI`（仅 AI 用）：🦖 坐骑 1 → 1/3（0.33） | ✅ |
| 无敌（彩虹）概率 -1/2 | AI 表 🌈 0.01 → 0.005 | ✅ |
| 靴子（疾跑鞋）概率 -2/3 | AI 表 👟 13 → 13/3（4.33） | ✅ |
| 其它各减 1/2（爆炸药/炸弹/手套/心） | AI 表 🧪 2→1、💣 15→7.5、🧤 7→3.5、❤️ 7→3.5；差额并入"无道具" 79.83% | ✅ |
| （实现方式）仅针对 AI | `destroySoftBlock` 按属主分支：**玩家炸 → 用地图预掷的隐藏道具**；**AI 炸（爆炸/坐骑顶开）→ 用 AI 掉落表重掷**；`tickBombs`/`tickPlayerTimers` 传入 sessionRng；`rollBlockDrop(rng, table?)` 支持传表 | ✅ |

验证：88 项单测全过（新增：AI 表权重断言、rollBlockDrop 传表确定性、AI 破坏重掷端到端、5 分钟时长）；AI 30 种子扫描 **zero=0/30、破墙 30.5/45.6（67%）**；单文件构建 67.7 kB

### 2026-08-16 · v1.5.6 软墙 45±2 + 连通含软墙 + 安全区 + 踢泡终点规则（本轮）

| 需求 | 实施 | 状态 |
|---|---|---|
| 软墙增加到 45 左右 ±2 | 软墙生成重做：先放四边连通路径（内圈）软墙 → 散点区**最大覆盖贪心**预留安全区（每轮选覆盖最多未覆盖格的格，安全区最少、软墙最多）→ 其余非硬墙格全放软墙 → 目标裁剪 → 稳定化后**补足循环**（补放+还原多轮收敛到几何上限）；30 种子实测均值 **EASY 46.0 / NORMAL 45.6 / HARD 44.1**（区间 41~47，全部落在 45±2） | ✅ |
| 连通区域的计算包含软墙（软墙也算连通区域） | 修正理解：`floodReachable` 以**软墙可通行**计算连通（空地+软墙整体连通，口袋只可能被硬墙围死）；连通性/校准/稳定化循环均按此 | ✅ |
| 软墙的设置必须考虑是否存在安全区域（人物可站） | 每个软墙都紧邻"安全区/常空区"空地：贪心预留安全区按构造保证 + 放置后安全检查（无空地邻居的软墙还原为空地）+ 稳定化循环兜底；30 种子 × 3 难度 badSoft=0 | ✅ |
| 踢炸弹规则变更：终点为 AI 所在格子的前一个格子；否则被墙挡住的格子的前一格子 | `computeMove` 推泡首格 + `canEnterCellForKick` 滑动格均把**角色（玩家/AI）当阻挡**：前进格被角色占据 → 泡泡不能再前进（停在角色的前一格）；被墙/软墙/泡泡挡住 → 停在挡住格的前一格（原有行为）；移动系统传入 players | ✅ |
| （配套）硬墙占比微调 | NORMAL 硬墙目标 24→22 对（总 96，仍在 48%–55% 区间内），给软墙腾空间；硬墙生成算法未动 | ✅ |

验证：84 项单测全过（新增：推泡被角色挡停 2 项、滑动停 AI 前一格端到端 1 项、软墙 ≥40）；硬墙 48.2%~51.3%、无 3×3、2×2 ≤10、badSoft=0、connFail=0；AI 30 种子扫描 **zero=0/30、破墙 30.4/45.6 块（67%）**；单文件构建 67.4 kB

### 2026-08-16 · v1.5.5 软墙加到 ~35，四边连通路径上也放（本轮）

| 需求 | 实施 | 状态 |
|---|---|---|
| 软墙再加，加到 35 左右 | 软墙扩量：散点区贴常空区密度 0.65/0.6/0.55 + 第二遍补放（×0.8）+ 保底提到 12 对；30 种子实测均值 **EASY 36.4 / NORMAL 34.4 / HARD 32.0**（区间 24~48） | ✅ |
| 可在四边的连通路径上加软墙 | 新增**内圈行/列软墙**（密度 0.55/0.5/0.45 + 环线保底 ≥8 格）；出生区 3×3 仍常空（含内圈格）；"四边为通路"测试放宽为"空地或可炸软墙"，并新增出生区保护断言 | ✅ |
| （附带修复）校准补硬墙后软墙失去唯一空地邻居/产生新口袋 | 尾段改为**稳定化循环**（最多 4 轮）：软墙安全检查 → 连通性拆墙（返回是否拆过）→ 硬墙校准，直到一轮无变化；`ensureConnectivity` 改为返回 boolean；30 种子 × 3 难度 badSoft=0、connFail=0 | ✅ |

验证：81 项单测全过（软墙下限提到 22）；硬墙占比 48.2%~53.3% 与无 3×3 / 2×2≤12 全部保持；AI 30 种子扫描 **zero=0/30、破墙 26.5/34.4 块（77%）**；单文件构建 66.0 kB

### 2026-08-16 · v1.5.4 硬墙生成不动，多多加可破坏墙体（本轮）

| 需求 | 实施 | 状态 |
|---|---|---|
| 硬墙生成不用动，在此基础上多多加可破坏墙体 | **硬墙生成完全保留**（散点约束 + 48%–55% + 棋盘保底 + 围死守卫 + 校准，实测 EASY 94–96 / NORMAL 94–100 / HARD 98–104 不变）；软墙由"只贴常空区 + 密度 0.4~0.45 + 保底 8 对"扩为：①贴常空区密度提至 0.5~0.6 → ②**其余空位第二遍补放**（密度减半，被安全检查还原的自动变回空地，不影响硬墙）→ ③**保底下限提到 10 对**（接受"有相邻空地"的空位，放置后空地不会再变必存活） | ✅ |
| （附带修复）散点+软墙后出现"空地口袋"未被拆开 | `ensureConnectivity` 原来只查口袋种子格的**直接墙邻居**，多格口袋（空+软混合）拆墙失败空转 400 轮；改为**洪泛整个口袋区域**再拆边界"贴着可达区"的墙（否则拆任意边界墙），30 种子 × 3 难度 connFail=0 | ✅ |

验证：81 项单测全过；软墙 30 种子实测 **EASY ≥18 / NORMAL ≥16 / HARD ≥16**（典型 18~22，较上版 10~16 明显增多），badSoft=0；硬墙占比与无 3×3 / 2×2≤12 全部保持；AI 30 种子扫描 **zero=0/30、破墙 15.9/19.3 块（82%）**；单文件构建 65.3 kB

### 2026-08-16 · v1.5.3 地图再改：硬墙散点化（无 3×3、2×2 少），占比 48%–55%（本轮）

| 需求 | 实施 | 状态 |
|---|---|---|
| 不要出现 3×3 连续硬方块，2×2 也尽量少 | map.ts 改为**约束散点放置**：`wouldCreate3x3`（严格禁止，30 种子 × 3 难度 max3x3=0）+ `wouldCreate2x2`（50% 概率拒绝，实测 0~12 个 2×2 窗口）+ **棋盘格位保底填充**（天然无 2×2/3×3 且 180° 对称，解决随机散点 3×3 死锁）+ `wouldCreatePocket`（防止硬墙围死空位） | ✅ |
| 硬方块数量变为 48%–55%（占总格） | 目标：EASY 22 / NORMAL 24 / HARD 26 对（外圈 52 + 内部），再加**连通性后校准**（拆墙削量对称补回，下限 94 = 48%）；30 种子实测：EASY 94–96（48.2%–49.2%）/ NORMAL 94–100（48.2%–51.3%）/ HARD 96–104（49.2%–53.3%） | ✅ |
| （附带）软墙不因密集硬墙被安全检查清光 | 软墙**优先贴常空区**（四边通路/中心广场/出生区）放置——一定有常空邻居不会被还原，保底 8 对；实测软墙 ≥10 格、badSoft=0 | ✅ |

验证：81 项单测全过（新增：硬墙 48%–55% 区间、无 3×3、2×2 ≤14）；AI 30 种子扫描 **zero=0/30、破墙 13.2/15.7 块（84%）**；单文件构建 64.8 kB

### 2026-08-16 · v1.5.2 地图再改：不可破坏 ≥ 1/2，先通路后障碍（本轮）

| 需求 | 实施 | 状态 |
|---|---|---|
| 地图中不可破坏的格子太少，起码占全部格子的 1/2 | map.ts 重写为**经典泡泡堂式**：先构成通路（中心 3×3 广场 + 十字通路 + 成对死胡同分支 + 四边内圈通路），**其余内部全部硬墙**；推演保证 `硬墙 = 116 − softTarget + 2×通路软墙对数 ≥ 98 格`（50.3%+），30 种子实测 EASY≥100 / NORMAL≥98 / HARD≥98（50.3%~59%） | ✅ |
| 构成通路以后，设置可炸的障碍（放炸弹的人有安全格子可以站） | 软墙铺在**通路格上**（密度随难度 0.4/0.5/0.6 + 保底对数 2/4/6）+ 保底补充到 20/26/30 格；每个软墙必须有 ≥1 个相邻空地（安全检查把"四邻全非空地"的软墙还原为通路），**放炸弹的人总有安全格可站**（30 种子 badSoft=0） | ✅ |

验证：80 项单测全过（新增：硬墙 ≥1/2、软墙 ≥18、每软墙有安全格）；AI 30 种子扫描 **zero=0/30、破墙 21.0/24.8 块（85%）**；单文件构建 63.4 kB

### 2026-08-16 · v1.5.1 地图再改（开阔+软墙多）+ 彩虹 0.01% 且无敌（本轮）

| 需求 | 实施 | 状态 |
|---|---|---|
| 地图再改：可走的路径太少、可炸的格子也太少（参考 10×9 泡泡堂实例，尺寸保持 15×13） | map.ts 重写：外圈硬墙封闭；内部**默认全空地**（路径开阔），软墙随机对称铺放（密度 0.42/0.52/0.62）+ **保底补充到 24/30/36 格**（参考实例约 30% 木箱）；内部硬墙仅**少量对称结构**（EASY 4 / NORMAL 6 / HARD 8 格）；中心 3×3 广场常空、四边内圈通路、出生区 3×3 常空、180° 中心对称、连通性保证不变 | ✅ |
| 彩虹出现概率降低到 0.01% | 掉落表 🌈 3% → **0.01%**（并入无道具 54.99%，合计仍 100） | ✅ |
| 彩虹期间无敌 | `resolveHit` 入口拦截：`rainbow` 期间免疫爆炸/接触伤害、不被泡封、不消耗坐骑（爆炸与彩虹接触伤害都走此函数） | ✅ |
| （验证）新地图下 AI 表现 | 30 种子扫描：**zero=0/30、平均破墙 33.2/41.2 块（约 80%）**——开阔地图 AI 清墙效率大幅提升 | ✅ |

验证：79 项单测全过（新增：内部硬墙 ≤12 且 ≥2、软墙 ≥28、彩虹权重 0.01、彩虹无敌）；单文件构建 63.0 kB

### 2026-08-16 · v1.5 密集地图重做 + 敌人掉落 + 彩虹 20 秒（本轮）

| 需求 | 实施 | 状态 |
|---|---|---|
| 地图太"空旷"要重改：至少 13/22 格子不可破坏；泡泡堂式死胡同带连通、蜿蜒、仍中心对称；四周和中心有通路；中心区域 4 或 9 格总是空出来 | map.ts 重写：**先全填硬墙再挖通路模板**（中心 3×3 广场常空 + 十字通路 + 成对死胡同分支），软墙密度 0.28/0.36/0.44（EASY/NORMAL/HARD），连通拆墙对称镜像；出生区 3×3 常空；测试验证硬墙占比 ≥13/22、中心广场常空、中心对称 | ✅ |
| 炸死敌人掉道具：吃过的道具掉落概率高、没吃的也有小概率、什么也没吃不掉落 | `dropEnemyLoot`（tickExplosions 内）：按敌人 `eaten` 记录逐类型判定——吃过 60% / 没吃 8% 掉一颗；从未吃任何道具的敌人不掉落；掉落道具在爆炸生成后放置（不受同轮水柱销毁） | ✅ |
| 彩虹持续时间 20 秒 | `rainbowTimer=1200` tick（20s），倒计时归零彩虹失效；吃新的彩虹刷新计时 | ✅ |
| （附带修复）密集地图下 AI 在死胡同口袋逐 tick 左右乒乓、破墙效率暴跌（zero=4/30、avgBlocks=1.4） | 根因：贪心直走优先，走进口袋后贪心与 BFS 来回横跳，且每 tick 都在动导致卡住检测不触发。修复：贪心**禁止回退到刚离开的格**（`prevCell`，ai-system 记录上一 tick 格），绕路交给 BFS（路径一致不乒乓） | ✅ |

验证：30 种子扫描 **zero=0/30、平均破墙 6.2 块（soft≈8）**；77 项单测全过；单文件构建 62.6 kB

### 2026-08-16 · v1.4.9 地图重做（泡泡堂蜿蜒死胡同风格）+ 彩虹光波（本轮）

| 需求 | 实施 | 状态 |
|---|---|---|
| 障碍不要"一格一格"棋盘式，要死胡同带连通（参考 Q 版泡泡堂，可蜿蜒、仍中心对称），重改地图 | 内部障碍改为**对称随机墙段**（10~14 段蜿蜒短墙，自然形成死胡同与连通走廊），非棋盘式；外圈+四边通路+中心对称保留，连通性拆墙也成对镜像 | ✅ |
| 炸弹威力最高为 4 | `maxRange 6→5→4` | ✅ |
| 炸弹（可用数）道具概率 +5% | 掉落表 💣 10%→15% | ✅ |
| 威力道具概率 -5% | 掉落表 🧪 7%→2% | ✅ |
| 新增彩虹光波：身体覆彩虹，碰到的人物掉 1 格命，移速 +10%，按 Z/Y 与恐龙一起掉落 | 新道具 `RAINBOW`（掉落 3%）：同格接触 → 对方掉 1 格命（无敌可挡）；移速 ×1.1；彩虹光环渲染；Z/Y 作弊同时生成恐龙+彩虹 | ✅ |

验证：浏览器实测墙布局非棋盘、中心对称、Z 键双掉落、彩虹接触即死；30 种子 AI 扫描卡死 0/30

### 2026-08-16 · v1.4.8 地图重构（关卡2风格）+ AI 徘徊自检（较早轮次）

| 需求 | 实施 | 状态 |
|---|---|---|
| 更喜欢关卡2的地图风格 | 所有关卡固定 **15×13**（关卡2尺寸） | ✅ |
| 四个角各生成一个人物，1234 组成矩形 | 四角出生：1 玩家(左上) + 3 敌人(右上/右下/左下)，每关 4 人 | ✅ |
| 矩形四边生成通路（12/23/34/41） | 内圈行 1/11、列 1/13 全部留空为通路，四角沿边连通 | ✅ |
| 中心对称，把地图分成四块 | 内部棋盘式硬墙 + 软墙 + 隐藏道具 **180° 中心对称生成**（镜像同步） | ✅ |
| 每一关地图矩阵都应如此生成 | 每关统一按此规格生成（软墙/道具随种子变化，布局对称） | ✅ |
| AI 自检测是否卡住/徘徊，是则重新设定目标 | 徘徊检测：最近 80 tick 回到同一格 + 80 tick 无破墙进展 → 判定徘徊，记入"最近尝试过的墙"并排除、换新目标 | ✅ |

验证：30 种子扫描 **卡死 0/30、零空种子、平均破墙 4.7 块**；浏览器实测 15×13、4 角角色、中心对称、四边通路

### 2026-08-16 · v1.4.7 音乐/数值/AI 攻击/表现调整（较早轮次）

| 需求 | 实施 | 状态 |
|---|---|---|
| 暂停时音乐音量降为 20%，继续恢复 | `AudioService.setMusicVolume`：暂停 → 0.2，继续 → 1（BGM 每小节按系数缩放） | ✅ |
| AI 没有墙可炸就炸人（互相攻击/攻击玩家） | 攻击分支：`没有可炸的墙` → 无视距离主动追玩家（贴近放泡/追击）；有墙时仍以破墙为主 | ✅ |
| 背着向上走脸上还有嘴 | 普通角色朝上时嘴（含眼睛/腮红）不画——背面视角不画脸 | ✅ |
| 速度靴加成减小；速度上限 5、威力上限 5、最多 6 个泡泡 | 疾跑鞋 ×1.15→×1.08；`maxSpeed 5.5→5`、`maxRange 6→5`、`maxMaxBombs 6`（保持） | ✅ |
| 恐龙高大一点（现在像狗） | 重绘恐龙：更高更圆润的躯干、脖子、更大头部 + 脑后头冠、翘尾，骑手骑在更高的背上 | ✅ |

### 2026-08-16 · v1.4.6 AI 卡死修复 + 背面视角不画脸（较早轮次）

| 需求 | 实施 | 状态 |
|---|---|---|
| AI 有时卡住不动（非等爆炸）或来回徘徊 | 根因：目标墙被硬墙/软墙围死（无可走相邻格）时寻路永远失败 → 决策"原地等"且故意站定不累计卡住 → 目标墙永不更换 → 永久卡死。修复：①选目标墙只选"**可到达**"的（有可走相邻格）；②**静止超时 15 tick**（非等爆炸）强制换目标/移动；③贴墙放不了泡且卡住时退开 | ✅ |
| 骑恐龙往上走角色还有眼睛（吓人） | 朝上 = 背面视角：恐龙眼睛、骑手眼睛/腮红、普通角色眼睛/腮红在朝上时一律不画 | ✅ |

验证：30 种子扫描 **卡死 0/30、零空种子、平均破墙 5.1 块**（修复前 15/30 卡死、多个种子 0 块）

### 2026-08-16 · v1.4.5 恐龙机制完善 + AI 静止不抽搐（较早轮次）

| 需求 | 实施 | 状态 |
|---|---|---|
| 骑恐龙被炸：恐龙不应还在（可抵一次扣血后消失） | `resolveHit`：骑坐骑被炸 → 抵一次伤害（不扣生命）+ 2s 无敌，恐龙消失（mount=false），破墙中断 | ✅ |
| 不能自动破砖，需方向键命令（按住 0.5s）才破 | 破墙分两阶段：`charge`（按住 0.5s 充能，松开/换方向即取消）→ `hit`（破墙 1.5s，完成才顶开）；轻触/走上去不破 | ✅ |
| AI 哪怕静止也不能抽搐 | 卡住检测改为"想移动却没移动"才累计；故意站定（等待/贴墙等待）清零 → 静止时绝不被强制换向；浏览器实测 2.49s 逐点完全静止 | ✅ |
| AI 也要会骑恐龙，不能骑着就不操作 | 坐骑 AI 破墙走"顶墙"路线：贴到软墙直接顶（充能+破墙），破墙中持续按住方向；浏览器实测 AI 骑恐龙 10s 破 3 块 | ✅ |
| 恐龙出现概率降低 | `BLOCK_DROP_TABLE`：坐骑 2%→1%（无道具 54→55） | ✅ |

### 2026-08-16 · v1.4.4 恐龙具象化 + AI 移动连贯（较早轮次）

| 需求 | 实施 | 状态 |
|---|---|---|
| AI 等待炸弹爆炸有点跳格子，别直接吸附，移动要连贯 | 移除等待分支的直接吸附；改为"若正处在两格之间，先沿当前方向滑到格心站定再等待"——30ms 采样 200 点零跳变 | ✅ |
| 恐龙具象化：小人骑在恐龙身体上 | `drawDinoRider`：Q 版绿色恐龙（身体/腿/尾巴/头/吻部/眼睛）+ 背上缩小版小人骑手（带朝向眼睛/腮红），替代原 🦖 emoji | ✅ |
| 骑恐龙破一个格子要 1.5s，有过程动画 | `Player.break` 破墙状态（90 tick）：顶到软墙后锁移动开始破墙（按其他方向可取消）；完成才顶开（掉道具+计分）；动画：恐龙朝目标顶撞摆动 + 软墙抖动 + 裂纹随进度增多 | ✅ |

### 2026-08-16 · v1.4.3 掉落概率调整 + Z/Y 作弊 + AI 清场优化（较早轮次）

| 需求 | 实施 | 状态 |
|---|---|---|
| 爆炸药（威力）概率减半；其他物品（除坐骑）概率减 1/3 | `BLOCK_DROP_TABLE`：爆炸药 15→7、炸弹 15→10、鞋 20→13、手套 10→7、心 10→7、坐骑 2 不变、无道具 28→54（合计 100） | ✅ |
| 按 Z/Y 键在周围空格自动生成坐骑（测试用，不写入规则） | 表现层作弊：按 Z 或 Y 在玩家周围 2 格内空地生成 🦖 坐骑（`app.ts`，非 core 规则） | ✅ |
| AI 仍有徘徊效果；等待时不要抽搐（反复变换方向），要在格子中心等待 | ①破墙改为「贴到任意软墙就放泡/原地等待」，不再绕路、不再错过贴墙窗口；②等待期强制吸附格心、返回空闲（不换方向）；③卡住时贴墙也不换目标 | ✅ |
| AI 主目标=炸掉所有可炸格子；看到玩家才追一下 | 破墙主目标优先；攻击分支改为「视野 ≤5 格 且 玩家比最近墙更近」才追击/放泡（记住这一点） | ✅ |

### 2026-08-16 · v1.4.2 泡封挣脱 + AI 不再反复移动忘记目标（较早轮次）

| 需求 | 实施 | 状态 |
|---|---|---|
| 被炸到的硬直泡泡可通过按上下左右（各一遍）突破 | 新增 `tryTrapEscape`：泡封期间每按过一个不同方向记一次，四个方向各按一遍立即挣脱（附 2s 无敌）；按不满就等自动解除 | ✅ |
| AI 有时反复移动忘记目标 | ①**目标墙承诺制**：锁定一堵墙直到被炸掉，不再每次决策重选最近的墙（防两墙间横跳）；②贴墙后只「放泡或原地等待」，绝不瞎走；③**贴到目标墙立即打断方向保持**（修复：旧方向保持会让 AI 走过目标墙错过放泡窗口——这是反复漂移的根因）；④贴墙等待不算卡住、不换目标 | ✅ |

### 2026-08-16 · v1.4.1 AI 不再自爆（较早轮次）

| 需求 | 实施 | 状态 |
|---|---|---|
| AI 太容易被自己炸死：找安全的地方等自己的炸弹爆炸，别往危险区域去 | ①威胁区升级：即时危险(泡泡/水柱) + **所有泡泡的潜在爆炸范围（含连锁）**，AI 全程避开；②放泡前 BFS 确认爆炸范围外存在安全撤离点，没有就不放（被围死时宁可不炸）；③放泡后逃到爆炸范围外并**原地等待自己的泡泡爆炸完**再行动；④方向保持期内若前方进入威胁区立即改道 | ✅ |

### 2026-08-16 · v1.4 六项改进（较早轮次）

| 需求 | 实施 | 状态 |
|---|---|---|
| 每局游戏时间 +2 分钟 | `DIFFICULTY.timeLimitTicks`：简单 120→240s / 一般 90→210s / 困难 60→180s | ✅ |
| 暂停按钮 + 弹出操作/目标说明（点击任意处关闭） | HUD 左上 ⏸ 按钮；暂停弹层含🎯目标/🎮操作/💡道具提示说明，点击空白处或关闭按钮继续 | ✅ |
| 移动迷惑：从砖边缘滑到砖块上；踢泡要有一格一格的过程 | ①转向时垂直轴自动吸附进车道中心（两轴同时趋向目标格心），不再贴砖边移动；②踢泡改为「滑动推进」：被踢后以 7.5 格/秒 一格一格滑行（`kickTicks` 可调），撞墙/软墙/水柱/其他泡泡才停下，渲染插值平滑 | ✅ |
| AI 以「慢慢炸墙破墙」为主目标，顺便攻击玩家 | `decideAI` 优先级改为：逃跑 → 破墙（找最近软墙、贴墙放泡、反复推进）→ 攻击玩家（≤2 格放泡；身边无墙可破时主动靠近）→ 漫游 | ✅ |
| 2% 概率开出坐骑（恐龙：自动顶开软墙，移速 -10%） | 新增 `MOUNT` 道具（掉落权重 2%，无道具 30→28%）：坐骑可直接顶开软墙穿过（掉道具+计分），移速 ×0.9，头顶 🦖 + 绿色光环 | ✅ |
| 欢快背景音乐 | `WebAudioSynth.startMusic()`：C 大调五声音阶 16 步循环（旋律+低音+铃铛），音量轻，进关播放/回菜单停止 | ✅ |

### 2026-08-16 · v1.3 道具掉落 bug 修复 + 道具表调整（较早轮次）

| 需求 | 实施 | 状态 |
|---|---|---|
| 破坏方格半天一个道具也没有（bug） | 根因：`tickBombs` 先生成道具、后用同一轮爆炸的水柱格子过滤销毁道具 → 刚露出的道具被自己的爆炸删掉。修复：先清旧道具、再生成新道具，新道具存活 | ✅ |
| 手套概率变 10% | `BLOCK_DROP_TABLE`：KICK 10 | ✅ |
| 💣炸弹 → 可用炸弹数 +1 | `powerUpEffect(BOMB_COUNT)`：maxBombs +1（上限 6） | ✅ |
| 新增 🧪爆炸药 → 爆炸格子 +1（向四周） | `powerUpEffect(BOMB_RANGE)`：爆炸半径 +1（上限 6）；旧"炸弹=威力"更名为爆炸药 | ✅ |

新掉落表（合计 100，权重集中在 `BLOCK_DROP_TABLE` 可调）：
30% 无 / 15% 💣炸弹(可用数+1) / 15% 🧪爆炸药(威力+1) / 20% 👟疾跑鞋(+15%) / 10% 🧤手套 / 10% ❤️心

### 2026-08-16 · v1.2 道具重做 + 表现细节 + 地图多样（较早轮次）

| 需求 | 实施 | 状态 |
|---|---|---|
| 玩家脚下加光环，提示哪个是玩家 | `entity-renderer.drawPlayer`：玩家脚下金色脉冲光环（软光 + 圆环） | ✅ |
| AI 死后变透明表示死亡，头上加天使光环 | 死亡敌人整体 35% 透明度（幽灵质感）+ 头顶金色天使光环（椭圆环 + 光点） | ✅ |
| 破坏可破坏砖块后有概率生成道具：30% 无 / 20% 炸弹(威力+1) / 20% 疾跑鞋(+15%移速) / 20% 手套(踢泡) / 10% 心(回血) | `BLOCK_DROP_TABLE`（合计 100，NONE=30）；`powerUpEffect` 重做：💣 威力+1、👟 移速×1.15、🧤 踢泡、❤️ 回血+1（满血无效）；移除护盾/无敌星星/泡泡+1 三种旧道具 | ✅ |
| 爆炸特效：蓝色 → 红色 💥，且有冲击波感觉 | `drawExplosion`：红色主体（中心白热黄）、中心 💥 表情、双层扩散冲击波圆环；爆炸粒子改为红/橙/黄火星 | ✅ |
| 地图矩阵更多样（格式，非美术），保持可破坏通行区域连通 | `MAP_SIZES`（13×11 / 15×13 / 17×13 / 15×11 / 17×13 按关轮换）+ `MAP_PATTERNS`（棋盘 / 偏移棋盘 / 条纹 / 随机 按种子轮换）；`ensureConnectivity` 洪泛拆墙保证所有空地连通（软墙可炸视作可通行） | ✅ |

### 2026-08-16 · v1.1 移动 / 触屏 / AI 优化（较早轮次）

| 需求 | 实施 | 状态 |
|---|---|---|
| 移动 bug：贴墙卡死 / 两墙间斜穿 / 边界卡位 | 改为格心式移动：被挡回弹格心、自由转向、轴分离移动（杜绝斜切穿墙） | ✅ |
| AI 卡顿、不流畅 | 卡住检测改基于连续位移（修复每 3 tick 误判重决策）；方向保持窗口；贪心直走追击 + BFS 绕路；状态机（漫游/追击/攻击/逃跑） | ✅ |
| 手机模式无虚拟按键、未识别 | 触屏识别（pointer:coarse / ontouchstart）→ 显示虚拟摇杆（死区 10px）+ 放泡键；竖屏旋转提示 | ✅ |

---

## 待办（未来改进）

> 后续改进需求在此追加，例如：
> - 双人本地 PK 模式
> - 主动型道具（喷火/冰冻/手榴弹等）
> - 泡封营救（队友触碰解除）
> - 音效/背景音乐丰富、角色装扮系统
